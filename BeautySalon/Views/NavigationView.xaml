<UserControl x:Class="BeautySalon.Views.NavigationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:vm="clr-namespace:BeautySalon.ViewModels"
             xmlns:local="clr-namespace:BeautySalon.Views"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance {x:Type vm:NavigationViewModel}}"
             d:DesignHeight="450" d:DesignWidth="800">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadAsyncCmd}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border BorderBrush="Black" BorderThickness="0 0 1 0">
                <WrapPanel Orientation="Horizontal" Grid.Column="0" Margin="0" ItemHeight="125" ItemWidth="125">
                    <Button Style="{StaticResource NavigationBtn}" Command="{Binding OpenClientViewCmd}">
                        <Button.Content>
                            <StackPanel HorizontalAlignment="Stretch">
                                <Image  MaxHeight="90"/>
                                <TextBlock Text="Клиенты" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Button.Content>
                    </Button>

                    <Button Style="{StaticResource NavigationBtn}" Command="{Binding OpenOrderViewCmd}">
                        <Button.Content>
                            <StackPanel HorizontalAlignment="Stretch">
                                <Image MaxHeight="90"/>
                                <TextBlock Text="Записи" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Button.Content>
                    </Button>

                    <Button Style="{StaticResource NavigationBtn}" >
                        <Button.Content>
                            <StackPanel HorizontalAlignment="Stretch">
                                <!--<Image Source="/Resources/Images/fSHGT0HI8rA.jpg" />-->

                            </StackPanel>
                        </Button.Content>
                    </Button>

                    <Button Style="{StaticResource NavigationBtn}"/>
                </WrapPanel>
            </Border>

            <StackPanel Grid.Column="1">
                <StackPanel>
                    <Calendar Margin="10"/>
                </StackPanel>
                <StackPanel Margin="0 10">
                    
                </StackPanel>
            </StackPanel>

        </Grid>

    </Grid>
</UserControl>
