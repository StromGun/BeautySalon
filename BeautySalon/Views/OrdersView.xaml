<UserControl x:Class="BeautySalon.Views.OrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:BeautySalon.ViewModels"
             xmlns:con="clr-namespace:BeautySalon.Resources.Converters"
             xmlns:us="clr-namespace:BeautySalon.Resources.UserControls"
             xmlns:local="clr-namespace:BeautySalon.Views"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance {x:Type vm:OrdersViewModels}}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <!--<ItemsControl x:Name="IC" Grid.Row="1" ItemsSource="{Binding Orders}">

            --><!--<ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition />
                            <RowDefinition />              
                            <RowDefinition />
                            <RowDefinition />                                               
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                    </Grid>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            

            <ItemsControl.ItemContainerStyle>
                <Style>
                    <Setter Property="Grid.Column" Value="0" />
                    <Setter Property="Grid.Row" Value="{Binding TimeStart, Converter={con:DateToWeek}}" />
                </Style>
            </ItemsControl.ItemContainerStyle>--><!--

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    --><!--<Button Content="{Binding TimeStart, Converter={con:DateToWeek}}"/>--><!--
                    <Button Content="{Binding Path=DateStart}"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>

        </ItemsControl>-->


        <DataGrid Grid.Row="1" ItemsSource="{Binding Orders}" IsReadOnly="True">
            
        </DataGrid>

        <Button Grid.Row="2" HorizontalAlignment="Right" >
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Добавить"/>
            </StackPanel>
        </Button>
    </Grid>
</UserControl>
